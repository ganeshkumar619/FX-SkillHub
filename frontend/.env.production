# Production environment — committed to source control.
# This file is loaded by Vite during `npm run build` (mode=production).
# Values are baked into the compiled bundle at build time.
#
# Do NOT put secrets here. This is a public URL only.
VITE_API_BASE_URL=https://fx-skillhub.onrender.com/api
